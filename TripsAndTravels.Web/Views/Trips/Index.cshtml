@model IEnumerable<TripsAndTravels.Web.Data.Entities.TripEntity>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTrip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDateTrip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDateTrip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DestinyCity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdTrip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDateTrip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDateTrip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DestinyCity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
